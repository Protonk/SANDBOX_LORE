# Agents in `book/graph/concepts/`

Purpose:
- Holds the concept inventory sources (`CONCEPT_INVENTORY.md`, `validation/Concept_map.md`) and generated JSON (`concepts.json`, `concept_map.json`, `concept_text_map.json`).
- Strategies (`validation/strategies.json`) are generated by the Swift tool.

Do:
- Edit markdown sources when changing definitions or validation intent.
- Regenerate JSON via `swift run` from `book/graph/`.

Don’t:
- Manually edit generated JSON; instead adjust sources and rerun.
- Invent new concept names without aligning with `CONCEPT_INVENTORY.md`.
